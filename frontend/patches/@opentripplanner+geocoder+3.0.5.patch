diff --git a/node_modules/@opentripplanner/geocoder/esm/apis/photon/index.js b/node_modules/@opentripplanner/geocoder/esm/apis/photon/index.js
index 8f15cf1..cc5a03f 100644
--- a/node_modules/@opentripplanner/geocoder/esm/apis/photon/index.js
+++ b/node_modules/@opentripplanner/geocoder/esm/apis/photon/index.js
@@ -69,28 +69,17 @@ function _autocomplete() {
             limit: size,
             q: text
           };
-          if (!focusPoint) {
-            _context.next = 10;
-            break;
+          // Add focusPoint if provided (bias toward this location)
+          if (focusPoint) {
+            _normalize3 = normalize(focusPoint), lat = _normalize3.lat, lon = _normalize3.lon;
+            query.lat = lat.toString();
+            query.lon = lon.toString();
           }
-          _normalize3 = normalize(focusPoint), lat = _normalize3.lat, lon = _normalize3.lon;
-          query.lat = lat.toString();
-          query.lon = lon.toString();
-          _context.next = 8;
-          return run({
-            options: options,
-            query: query,
-            url: url
-          });
-        case 8:
-          res = _context.sent;
-          return _context.abrupt("return", res);
-        case 10:
+          // Add boundary filter if provided (restrict to this area)
           if (boundary) {
             country = boundary.country, rect = boundary.rect;
-            if (country) query["in"] = "countryCode:".concat(country);
             if (rect) {
-              query["in"] = "bbox:".concat([rect.minLon, rect.minLat, rect.maxLon, rect.maxLat].join(","));
+              query.bbox = [rect.minLon, rect.minLat, rect.maxLon, rect.maxLat].join(",");
             }
           }
           return _context.abrupt("return", run({
diff --git a/node_modules/@opentripplanner/geocoder/esm/geocoders/pelias.js b/node_modules/@opentripplanner/geocoder/esm/geocoders/pelias.js
index 701ebff..629f6f5 100644
--- a/node_modules/@opentripplanner/geocoder/esm/geocoders/pelias.js
+++ b/node_modules/@opentripplanner/geocoder/esm/geocoders/pelias.js
@@ -30,6 +30,28 @@ var PeliasGeocoder = /*#__PURE__*/function (_Geocoder) {
   }
   _inherits(PeliasGeocoder, _Geocoder);
   return _createClass(PeliasGeocoder, [{
+    key: "autocomplete",
+    value:
+    /**
+     * Override autocomplete to fall back to /v1/search (which uses libpostal)
+     * when autocomplete returns no results.
+     */
+    function autocomplete(query) {
+      var _this = this;
+      return this.api
+        .autocomplete(this.getAutocompleteQuery(query))
+        .then(this.rewriteAutocompleteResponse.bind(this))
+        .then(function (response) {
+          var features = response === null || response === void 0 ? void 0 : response.features;
+          if (features && features.length === 0 && query.text && query.text.length >= 8) {
+            return _this.api
+              .search(_this.getSearchQuery({ text: query.text }))
+              .then(_this.rewriteAutocompleteResponse.bind(_this));
+          }
+          return response;
+        });
+    }
+  }, {
     key: "getAutocompleteQuery",
     value:
     /**
diff --git a/node_modules/@opentripplanner/geocoder/esm/geocoders/photon.js b/node_modules/@opentripplanner/geocoder/esm/geocoders/photon.js
index 8825cf4..287ea55 100644
--- a/node_modules/@opentripplanner/geocoder/esm/geocoders/photon.js
+++ b/node_modules/@opentripplanner/geocoder/esm/geocoders/photon.js
@@ -14,11 +14,23 @@ import Geocoder from "./abstract-geocoder";
 
 var generateLabel = function generateLabel(properties) {
   var propertyList = [];
+  // Combine housenumber with street for better address formatting
+  var housenumber = properties.housenumber ? "".concat(properties.housenumber) : "";
+  var street = properties.street ? "".concat(properties.street) : "";
+  var streetAddress = "";
+  if (housenumber && street) {
+    streetAddress = "".concat(housenumber, " ").concat(street);
+  } else if (street) {
+    streetAddress = street;
+  } else if (housenumber) {
+    streetAddress = housenumber;
+  }
+  var modifiedProperties = _objectSpread(_objectSpread({}, properties), {}, { street: streetAddress });
   ["name", "street", "district", "state", "postcode", "city", "country"].forEach(function (propertyName) {
-    if (typeof properties[propertyName] === "undefined") {
+    if (typeof modifiedProperties[propertyName] === "undefined") {
       return;
     }
-    var value = "".concat(properties[propertyName]);
+    var value = "".concat(modifiedProperties[propertyName]);
     if (value.length > 0) {
       propertyList.push(value);
     }
diff --git a/node_modules/@opentripplanner/geocoder/lib/apis/photon/index.js b/node_modules/@opentripplanner/geocoder/lib/apis/photon/index.js
index 48aedf7..043b9ef 100644
--- a/node_modules/@opentripplanner/geocoder/lib/apis/photon/index.js
+++ b/node_modules/@opentripplanner/geocoder/lib/apis/photon/index.js
@@ -82,26 +82,22 @@ function autocomplete(_a) {
             switch (_e.label) {
                 case 0:
                     query = { limit: size, q: text };
-                    if (!focusPoint) return [3 /*break*/, 2];
-                    _d = (0, lonlat_1.normalize)(focusPoint), lat = _d.lat, lon = _d.lon;
-                    query.lat = lat.toString();
-                    query.lon = lon.toString();
-                    return [4 /*yield*/, run({ options: options, query: query, url: url })];
-                case 1:
-                    res = _e.sent();
-                    return [2 /*return*/, res];
-                case 2:
+                    // Add focusPoint if provided (bias toward this location)
+                    if (focusPoint) {
+                        _d = (0, lonlat_1.normalize)(focusPoint), lat = _d.lat, lon = _d.lon;
+                        query.lat = lat.toString();
+                        query.lon = lon.toString();
+                    }
+                    // Add boundary filter if provided (restrict to this area)
                     if (boundary) {
                         country = boundary.country, rect = boundary.rect;
-                        if (country)
-                            query.in = "countryCode:".concat(country);
                         if (rect) {
-                            query.in = "bbox:".concat([
+                            query.bbox = [
                                 rect.minLon,
                                 rect.minLat,
                                 rect.maxLon,
                                 rect.maxLat
-                            ].join(","));
+                            ].join(",");
                         }
                     }
                     return [2 /*return*/, run({ options: options, query: query, url: url })];
diff --git a/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js b/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js
index 2244aec..587f6eb 100644
--- a/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js
+++ b/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js
@@ -42,6 +42,25 @@ var PeliasGeocoder = /** @class */ (function (_super) {
     function PeliasGeocoder() {
         return _super !== null && _super.apply(this, arguments) || this;
     }
+    /**
+     * Override autocomplete to fall back to /v1/search (which uses libpostal)
+     * when autocomplete returns no results.
+     */
+    PeliasGeocoder.prototype.autocomplete = function (query) {
+        var _this = this;
+        return this.api
+            .autocomplete(this.getAutocompleteQuery(query))
+            .then(this.rewriteAutocompleteResponse.bind(this))
+            .then(function (response) {
+                var features = response === null || response === void 0 ? void 0 : response.features;
+                if (features && features.length === 0 && query.text && query.text.length >= 8) {
+                    return _this.api
+                        .search(_this.getSearchQuery({ text: query.text }))
+                        .then(_this.rewriteAutocompleteResponse.bind(_this));
+                }
+                return response;
+            });
+    };
     /**
      * Generate an autocomplete query specifically for the Pelias API. The
      * `sources` parameter is a Pelias-specific option.
diff --git a/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js.orig b/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js.orig
new file mode 100644
index 0000000..2244aec
--- /dev/null
+++ b/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js.orig
@@ -0,0 +1,111 @@
+"use strict";
+var __extends = (this && this.__extends) || (function () {
+    var extendStatics = function (d, b) {
+        extendStatics = Object.setPrototypeOf ||
+            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
+            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
+        return extendStatics(d, b);
+    };
+    return function (d, b) {
+        if (typeof b !== "function" && b !== null)
+            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
+        extendStatics(d, b);
+        function __() { this.constructor = d; }
+        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
+    };
+})();
+var __assign = (this && this.__assign) || function () {
+    __assign = Object.assign || function(t) {
+        for (var s, i = 1, n = arguments.length; i < n; i++) {
+            s = arguments[i];
+            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
+                t[p] = s[p];
+        }
+        return t;
+    };
+    return __assign.apply(this, arguments);
+};
+var __importDefault = (this && this.__importDefault) || function (mod) {
+    return (mod && mod.__esModule) ? mod : { "default": mod };
+};
+Object.defineProperty(exports, "__esModule", { value: true });
+var abstract_geocoder_1 = __importDefault(require("./abstract-geocoder"));
+var DEFAULT_LAYERS = "address,venue,street,intersection";
+/**
+ * Geocoder implementation for the Pelias geocoder.
+ * See https://pelias.io
+ *
+ * @extends Geocoder
+ */
+var PeliasGeocoder = /** @class */ (function (_super) {
+    __extends(PeliasGeocoder, _super);
+    function PeliasGeocoder() {
+        return _super !== null && _super.apply(this, arguments) || this;
+    }
+    /**
+     * Generate an autocomplete query specifically for the Pelias API. The
+     * `sources` parameter is a Pelias-specific option.
+     * This function fills in some more fields of the query
+     * from the existing values in the GeocoderConfig.
+     */
+    PeliasGeocoder.prototype.getAutocompleteQuery = function (query) {
+        var _a = this.geocoderConfig, apiKey = _a.apiKey, baseUrl = _a.baseUrl, boundary = _a.boundary, focusPoint = _a.focusPoint, _b = _a.layers, layers = _b === void 0 ? DEFAULT_LAYERS : _b, options = _a.options, sources = _a.sources;
+        return __assign({ apiKey: apiKey, boundary: boundary, focusPoint: focusPoint, layers: layers, options: options, 
+            // explicitly send over null for sources if provided sources is not truthy
+            // in order to avoid default isomorphic-mapzen-search sources form being
+            // applied
+            sources: sources || null, url: baseUrl ? "".concat(baseUrl, "/autocomplete") : undefined }, query);
+    };
+    /**
+     * Generate a search query specifically for the Pelias API. The
+     * `sources` parameter is a Pelias-specific option.
+     * This function fills in some more fields of the query
+     * from the existing values in the GeocoderConfig.
+     */
+    PeliasGeocoder.prototype.getSearchQuery = function (query) {
+        var _a = this.geocoderConfig, apiKey = _a.apiKey, baseUrl = _a.baseUrl, boundary = _a.boundary, _b = _a.layers, layers = _b === void 0 ? DEFAULT_LAYERS : _b, focusPoint = _a.focusPoint, options = _a.options, sources = _a.sources;
+        return __assign({ apiKey: apiKey, boundary: boundary, layers: layers, focusPoint: focusPoint, options: options, 
+            // explicitly send over null for sources if provided sources is not truthy
+            // in order to avoid default isomorphic-mapzen-search sources form being
+            // applied
+            sources: sources || null, url: baseUrl ? "".concat(baseUrl, "/search") : undefined, format: false }, query);
+    };
+    /**
+     * Rewrite the response into an application-specific data format using the
+     * first feature returned from the geocoder.
+     */
+    PeliasGeocoder.prototype.rewriteReverseResponse = function (response) {
+        var _a;
+        if ((_a = this.geocoderConfig) === null || _a === void 0 ? void 0 : _a.reverseUseFeatureCollection)
+            return response;
+        var _b = response.isomorphicMapzenSearchQuery.point, lat = _b.lat, lon = _b.lon;
+        var firstFeature = response[0];
+        return {
+            lat: lat,
+            lon: lon,
+            name: firstFeature.label,
+            rawGeocodedFeature: firstFeature
+        };
+    };
+    /**
+     * Remove duplicates based on name, which Pelias sometimes creates
+     */
+    PeliasGeocoder.prototype.rewriteAutocompleteResponse = function (response) {
+        var features = response === null || response === void 0 ? void 0 : response.features;
+        var filtered = features === null || features === void 0 ? void 0 : features.filter(function (feature, index) {
+            return !features
+                .slice(index + 1)
+                .some(function (f) {
+                var _a, _b, _c, _d;
+                return ((_a = f === null || f === void 0 ? void 0 : f.properties) === null || _a === void 0 ? void 0 : _a.name) === ((_b = feature === null || feature === void 0 ? void 0 : feature.properties) === null || _b === void 0 ? void 0 : _b.name) &&
+                    // This is unclean, but we know geometry will never contain objects
+                    JSON.stringify((_c = feature === null || feature === void 0 ? void 0 : feature.geometry) === null || _c === void 0 ? void 0 : _c.coordinates) ===
+                        JSON.stringify((_d = f === null || f === void 0 ? void 0 : f.geometry) === null || _d === void 0 ? void 0 : _d.coordinates);
+            });
+        });
+        return __assign(__assign({}, response), { features: filtered });
+    };
+    return PeliasGeocoder;
+}(abstract_geocoder_1.default));
+exports.default = PeliasGeocoder;
+//# sourceMappingURL=pelias.js.map
\ No newline at end of file
diff --git a/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js.rej b/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js.rej
new file mode 100644
index 0000000..1d8a38f
--- /dev/null
+++ b/node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js.rej
@@ -0,0 +1,15 @@
+--- node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js
++++ node_modules/@opentripplanner/geocoder/lib/geocoders/pelias.js
+@@ -54,7 +54,11 @@ var PeliasGeocoder = /** @class */ (function (_super) {
+             // in order to avoid default isomorphic-mapzen-search sources form being
+             // applied
+             sources: sources || null,
+-            url: baseUrl ? "".concat(baseUrl, "/autocomplete") : undefined }, query);
++            url: baseUrl ? (baseUrl.includes('/v1') 
++                ? "".concat(baseUrl, "/autocomplete")
++                : "".concat(baseUrl, "/v1/autocomplete")
++            ) : undefined
++        }, query);
+     };
+     /**
+      * Generate a search query specifically for the Pelias API. The
diff --git a/node_modules/@opentripplanner/geocoder/lib/geocoders/photon.js b/node_modules/@opentripplanner/geocoder/lib/geocoders/photon.js
index 9471e10..4af4f14 100644
--- a/node_modules/@opentripplanner/geocoder/lib/geocoders/photon.js
+++ b/node_modules/@opentripplanner/geocoder/lib/geocoders/photon.js
@@ -32,11 +32,25 @@ Object.defineProperty(exports, "__esModule", { value: true });
 var abstract_geocoder_1 = __importDefault(require("./abstract-geocoder"));
 var generateLabel = function (properties) {
     var propertyList = [];
+    // Combine housenumber with street for better address formatting
+    var housenumber = properties.housenumber ? "".concat(properties.housenumber) : "";
+    var street = properties.street ? "".concat(properties.street) : "";
+    var streetAddress = "";
+    if (housenumber && street) {
+        streetAddress = "".concat(housenumber, " ").concat(street);
+    }
+    else if (street) {
+        streetAddress = street;
+    }
+    else if (housenumber) {
+        streetAddress = housenumber;
+    }
+    var modifiedProperties = __assign(__assign({}, properties), { street: streetAddress });
     ["name", "street", "district", "state", "postcode", "city", "country"].forEach(function (propertyName) {
-        if (typeof properties[propertyName] === "undefined") {
+        if (typeof modifiedProperties[propertyName] === "undefined") {
             return;
         }
-        var value = "".concat(properties[propertyName]);
+        var value = "".concat(modifiedProperties[propertyName]);
         if (value.length > 0) {
             propertyList.push(value);
         }
diff --git a/node_modules/@opentripplanner/geocoder/src/apis/photon/index.ts b/node_modules/@opentripplanner/geocoder/src/apis/photon/index.ts
index 12fd2ca..508457e 100644
--- a/node_modules/@opentripplanner/geocoder/src/apis/photon/index.ts
+++ b/node_modules/@opentripplanner/geocoder/src/apis/photon/index.ts
@@ -68,8 +68,6 @@ async function autocomplete({
     const { lat, lon }: LonLatOutput = normalize(focusPoint);
     query.lat = lat.toString();
     query.lon = lon.toString();
-    const res = await run({ options, query, url });
-    return res;
   }
   if (boundary) {
     const { country, rect } = boundary;
diff --git a/node_modules/@opentripplanner/geocoder/src/geocoders/pelias.ts b/node_modules/@opentripplanner/geocoder/src/geocoders/pelias.ts
index 4063035..d0968f4 100644
--- a/node_modules/@opentripplanner/geocoder/src/geocoders/pelias.ts
+++ b/node_modules/@opentripplanner/geocoder/src/geocoders/pelias.ts
@@ -16,6 +16,27 @@ const DEFAULT_LAYERS = "address,venue,street,intersection";
  * @extends Geocoder
  */
 export default class PeliasGeocoder extends Geocoder {
+  /**
+   * Override autocomplete to fall back to /v1/search (which uses libpostal)
+   * when autocomplete returns no results. This handles cases where the
+   * simpler autocomplete parser can't normalize address variations
+   * (e.g. "West Old Shakopee Rd" vs "Old Shakopee Road West").
+   */
+  autocomplete(query: AutocompleteQuery): Promise<MultiGeocoderResponse> {
+    return this.api
+      .autocomplete(this.getAutocompleteQuery(query))
+      .then(this.rewriteAutocompleteResponse.bind(this))
+      .then((response: MultiGeocoderResponse) => {
+        const features = (response as any)?.features;
+        if (features && features.length === 0 && query.text && query.text.length >= 8) {
+          return this.api
+            .search(this.getSearchQuery({ text: query.text }))
+            .then(this.rewriteAutocompleteResponse.bind(this));
+        }
+        return response;
+      });
+  }
+
   /**
    * Generate an autocomplete query specifically for the Pelias API. The
    * `sources` parameter is a Pelias-specific option.
