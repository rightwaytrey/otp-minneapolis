# Global map hashing tweak: the otp site maps a long (48-char) cookie value,
# which overflows the default 64-byte bucket. Must load before any map block,
# so it lives here in conf.d (included before sites-enabled).
map_hash_bucket_size 128;
